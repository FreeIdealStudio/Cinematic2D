Initial Setup


Drop the Cinematic2D prefab onto the main camera.

Make sure that the camera culling ignores the new layer 'DoNotRender' (which is used by the focal point and background plane gizmos)
