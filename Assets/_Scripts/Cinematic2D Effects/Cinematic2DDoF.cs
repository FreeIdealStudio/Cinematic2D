using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//[ExecuteInEditMode] //Use to run while in edot mode
public class Cinematic2DDoF : MonoBehaviour {
	// Use this for initialization
	void Start () {

		
	}

    //get distances and work out value for DoF, then send to sprite to render
	
	// Update is called once per frame
	void Update () {
		
	}
}
